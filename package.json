{
  "name": "agent-routing-harness",
  "version": "0.1.0",
  "private": true,
  "description": "Repo-local AI-OS harness control plane consuming model-agnostic-workflow-system.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "harness:resolve": "node scripts/resolve-target-governance-zone.mjs",
    "harness:check": "npm test"
  },
  "engines": {"node": ">=20"}
}
